# ---------------------------------------------------------
# 1. NetBeans IDE 관련 (가장 중요)
# ---------------------------------------------------------
# 컴파일된 파일들이 모이는 곳 (지저분함)
/build/
/dist/
/nbdist/

# ★ 핵심: 사용자별 로컬 설정 (창 위치, 최근 열어본 파일, 내 컴퓨터 경로 등)
# 이 폴더는 절대 올리면 안 됨!
/nbproject/private/

# ---------------------------------------------------------
# 2. Java 컴파일 부산물
# ---------------------------------------------------------
*.class
*.log
*.ctxt
.mtj.tmp/

# ---------------------------------------------------------
# 3. 운영체제(OS) 생성 파일
# ---------------------------------------------------------
# Mac
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# ---------------------------------------------------------
# 4. 보안 및 환경 설정
# ---------------------------------------------------------
.env
!.env.example

# Python generated files
__pycache__/
*.pyc
venv/